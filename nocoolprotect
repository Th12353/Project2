-- Variables

local player = game.Players.LocalPlayer
local mouse = player:GetMouse()

-- Settings

bind1 = "n" -- has to be lowercase

-- Script

mouse.KeyDown:connect(function(ke1)
if ke1 == bind1 then
game.ReplicatedStorage.Protect:FireServer()
end
end)
